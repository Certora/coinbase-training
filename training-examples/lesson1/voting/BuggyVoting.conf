{
    // The name of the contract can be omitted
    // if it is the same as the file's name.
    "files": ["BuggyVoting.sol"],
    "verify": "BuggyVoting:SimpleRule.spec",
    "rule": ["voteIntegrity"],
    "rule_sanity": "basic",
    "solc": "solc8.0",  // Or use solc-select
    "msg": "Verifying a simple rule on a buggy Voting contract"
}
